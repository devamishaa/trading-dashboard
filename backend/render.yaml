services:
  - type: web
    name: trading-dashboard-backend
    env: go
    buildCommand: cd backend && go build -o trading-backend .
    startCommand: ./backend/trading-backend
    envVars:
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true

